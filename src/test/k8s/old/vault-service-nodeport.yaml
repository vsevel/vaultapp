apiVersion: v1
kind: Service
metadata:
  name: vault-public
  labels:
    app: vault
spec:
  type: NodePort
  ports:
    - name: vault
      port: 8200
      nodePort: 30200
  selector:
    app: vault